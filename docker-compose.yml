services:
  # Backend service (Node.js + Express + SQLite)
  backend:
    build:
      context: ./backend
    container_name: flow_backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
    volumes:
      - sqlite_data:/usr/src/app/data
    networks:
      - flow-network

  # Frontend service (Nginx distributes compiled React/Vite)
  frontend:
    build:
      context: ./app
    container_name: flow_frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
      - ollama
    networks:
      - flow-network

  # Ollama service
  ollama:
    image: ollama/ollama
    ports:
      - 11434:11434
    volumes:
      - ollama:/root/.ollama 
    networks:
      - flow-network
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]
    restart: unless-stopped
      

networks:
  flow-network:


# Defining named volumes managed by Docker Compose
volumes:
  sqlite_data:
  ollama:
    external: true